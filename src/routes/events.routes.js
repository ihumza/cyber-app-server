const {} = require('../controllers/events.controller');
const sanitize = require('../middlewares/sanitize-body');

const router = require('express').Router();

// router.get('/add', createEvent);

module.exports = router;
